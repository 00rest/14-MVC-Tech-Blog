document.getElementById('orest').innerText ='Hello js file!';


function commentBox(data){
    console.log(data)

};




// const commentSubmit = async (event) => {
//     if (event.target.hasAttribute('data-id')) {
//       const id = event.target.getAttribute('data-id');
  
//       const response = await fetch(`/api/projects/${id}`, {
//         method: 'DELETE',
//       });
  
//       if (response.ok) {
//         document.location.replace('/profile');
//       } else {
//         alert('Failed to delete project');
//       }
//     }
//   };








// document.querySelector('#').addEventListener('submit', commentSubmit);
// document.querySelector('.new-project-form').addEventListener('submit', commentSubmit);